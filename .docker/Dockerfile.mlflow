FROM ghcr.io/mlflow/mlflow:v2.19.0

# Install uv
COPY --from=ghcr.io/astral-sh/uv:latest /uv /uvx /bin/

# Install psycopg2-binary for PostgreSQL support using uv
RUN uv pip install --system psycopg2-binary==2.9.11
