# mlforge environment configuration
default_profile: dev

profiles:
  dev:
    offline_store:
      KIND: local
      path: ./feature_store
    online_store:
      KIND: redis
      host: localhost
      port: 6379
      prefix: mlforge
